apiVersion: v1
kind: Service
metadata:
    name: "hahaysh5aks"
    labels:
        app: "hahaysh5aks"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "hahaysh5aks"